# The self-contained macOS test bundle is large binary content. Store it in
# Git LFS so normal Git history stays small while a clone can hydrate the app.
"dist/Secunda Launcher.app/**" filter=lfs diff=lfs merge=lfs -text
